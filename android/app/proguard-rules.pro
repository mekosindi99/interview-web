# minifyEnabled is off for now (this is a thin WebView wrapper, nothing to
# shrink/obfuscate that would meaningfully change APK size) — kept as an
# empty file only because build.gradle's release buildType references it.
